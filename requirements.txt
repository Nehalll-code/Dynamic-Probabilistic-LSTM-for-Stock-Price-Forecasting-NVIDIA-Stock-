# Core
numpy>=1.23
pandas>=1.5
matplotlib>=3.5
seaborn>=0.12
scikit-learn>=1.1

# Deep learning
tensorflow>=2.10

# Data download + utilities
yfinance>=0.2.18
tqdm>=4.64

# Optional / convenience (nice to have)
ta>=0.10.0           # technical indicators (optional)
notebook              # if you want classic Jupyter
jupyterlab            # if you prefer JupyterLab
nbstripout            # strip outputs before committing notebooks

# For reproducible env (optional)
pip>=23.0
